---
import BaseLayout from '../layouts/BaseLayout.astro';
const steps = [['01','解压并启动','将 ZIP 完整解压到可写目录，运行 GenshinPiano.exe。'],['02','创建或导入','新建 .gpiano 曲谱，或直接拖入 MIDI、MusicXML 与旧版曲谱。'],['03','编辑并试听','在钢琴卷帘中调整音符、BPM、持续时间、音域与音色。'],['04','切换到游戏','按 F5 后聚焦游戏窗口，等待三秒倒计时开始演奏。']];
const shortcuts = [['Ctrl + N','新建曲谱'],['Ctrl + O','打开曲谱或 MIDI'],['Ctrl + Shift + S','另存为'],['Space','播放 / 暂停试听'],['F5','开始游戏演奏'],['Shift + F5','停止演奏'],['Ctrl + Z / Y','撤销 / 重做'],['Ctrl + 滚轮','水平缩放']];
---
<BaseLayout title="使用指南 · GenshinPiano" description="快速了解 GenshinPiano 的安装、曲谱导入、卷帘编辑、试听和游戏演奏流程。"><section class="page-hero shell"><span class="eyebrow">QUICK START</span><h1>从下载到第一首演奏</h1><p>四步完成安装、导入、编辑和游戏演奏。</p></section><section class="section shell"><div class="steps">{steps.map(([n,title,copy]) => <article><span>{n}</span><div><h2>{title}</h2><p>{copy}</p></div></article>)}</div><div class="guide-grid"><article class="card prose"><h2>安全演奏提示</h2><p>播放会等待国服 <code>YuanShen.exe</code> 或国际服 <code>GenshinImpact.exe</code> 获得焦点。失焦后自动暂停，返回窗口后继续。</p><p>如果游戏以管理员身份运行，请手动以管理员身份启动 GenshinPiano。软件不会自行申请提权。</p><p>演奏中按 <kbd>Esc</kbd> 可暂停并释放当前按键。</p></article><article class="card"><h2>常用快捷键</h2><dl class="shortcuts">{shortcuts.map(([key,label]) => <><dt><kbd>{key}</kbd></dt><dd>{label}</dd></>)}</dl></article></div><div class="center-action"><a class="text-link" href="https://github.com/tozyx/GenshinPiano/blob/main/docs/USER_GUIDE.md">阅读完整用户指南 →</a></div></section></BaseLayout>